{
  "version": "2.0.0",
  "tasks": [
    { "type": "npm", "script": "build", "problemMatcher": "$esbuild", "group": "build" },
    // Rebuilds on save. Reload the extension host window to pick it up.
    { "type": "npm", "script": "watch", "problemMatcher": "$esbuild-watch", "isBackground": true, "group": "build" }
  ]
}
