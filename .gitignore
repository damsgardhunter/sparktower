node_modules
dist
.env
.env.local
.env.development
# Catch .env.bak and friends — these hold live secrets.
.env.*
!.env.example
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
*.zip

# Files users have uploaded through the app. These are real people's
# documents — résumés, project files — and must never reach the repository.
local_objects

# Agent and editor configuration. .claude/settings.json holds a database URL.
.claude
.cursor
.aider*

# Replit and package-manager scratch directories.
.cache
.local
.upm
.config
.pythonlibs
.replit_cache

# Expo / React Native
mobile/node_modules
mobile/.expo
mobile/dist
mobile/ios
mobile/android

# Vitest
coverage/
